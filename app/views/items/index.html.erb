<header>
  <h1>Items</h1>

  <ul class="actions">
    <li>
      <%= link_to 'New Item', new_item_path, class: 'new' %>
    </li>
  </ul>
</header>

<div id="items">
  <% @items.each do |item| %>
    <h2><%= link_to item.name, item %></h2>
  <% end  %>
</div>

